#include <iostream>
using namespace std;

int main()
{
    int a, b, c, d, e, f, g, h;
    // User provides input
    cin >> a >> b >> c >> d >> e >> f >> g >> h;
    cout << b + d + f + h << " " << a + c + e + g << endl; 
    return 0; 
}