#include <iostream>
using namespace std;

int main()
{
    int max, x = 0;
    // user input
    cout << "Enter 10 numbers: ";
    cin >> x;
    max = x;

    cin >> x;
    if  (x > max) 
        max = x;
    cin >> x;
    if  (x > max) 
        max = x;
    cin >> x;
    if  (x > max) 
        max = x;
    cin >> x;
    if  (x > max) 
        max = x;
    cin >> x;
    if  (x > max) 
        max = x;
    cin >> x;
    if  (x > max) 
        max = x;
    cin >> x;
    if  (x > max) 
        max = x;
    cin >> x;
    if  (x > max) 
        max = x;
    cin >> x;
    if  (x > max) 
        max = x;
    
    cout << max << "\n";

    return 0;
}